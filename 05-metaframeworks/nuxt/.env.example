# Optional — copy to `.env` (Nuxt auto-loads `.env`, not `.env.local`) to
# override the defaults already set in nuxt.config.ts.

# -> runtimeConfig.public.apiUrl
NUXT_PUBLIC_API_URL=http://localhost:3001

# -> runtimeConfig.public.imageUrl
NUXT_PUBLIC_IMAGE_URL=http://localhost:3001
