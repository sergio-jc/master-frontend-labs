# Copy to `.env.local` and adjust if needed. Must use the PUBLIC_ prefix (see vite.config.ts).

# Base URL of the mock API server (../api-server)
PUBLIC_API_URL=http://localhost:3001

# Base URL used to build each house's image URL
PUBLIC_IMAGE_URL=http://localhost:3001
